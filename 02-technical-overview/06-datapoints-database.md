# Datapoints Database

Once the anomaly detector passes the tests, the reliable measurement is stored in the data points database. The next asynchronous steps are as follows:

*   Aggregate the data to perform H3 hexagon calculations.
*   Move the data to semi-cold storage on Coldline.
